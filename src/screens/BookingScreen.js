import { View, Text } from "react-native";

export default function BookingScreen() {
    return (
        <View style={{flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>Booking (rezervuoti trenerį, 7 dienos)</Text>
        </View>
    );
}